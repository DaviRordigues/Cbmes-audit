spring:
  application:
    name: cbmes-audit
  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/XE
    username: admin_user
    password: Oradoc_db1
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
